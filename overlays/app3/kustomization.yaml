apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: app2-anas

resources:
  - ../../base

patches:
  - target:
      kind: Cluster
      name: pg-cluster-ha
    patch: |
      apiVersion: postgresql.cnpg.io/v1
      kind: Cluster
      metadata:
        name: pg-cluster-ha-nexadd
      spec:
        instances: 1
